<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<title>Informations d'Identité</title>
		<style>
			body {
				font-family: Arial, sans-serif;
			}
			.container {
				width: 100%;
				margin: 0 auto;
			}
			.header {
				text-align: center;
				margin-bottom: 50px;
			}
			.header h1 {
				font-size: 24px;
			}
			.content {
				margin: 20px 0;
			}
			.content img {
				max-width: 200px;
				height: auto;
				display: block;
				margin: 0 auto;
			}
			.content p {
				font-size: 18px;
				text-align: center;
				margin: 10px 0;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<div class="header">
				<h1>Informations d'Identité</h1>
			</div>
			<div class="content">
				<p>
					{% if portefeuille.downloadURL %}
						<img src="{{ portefeuille.downloadURL }}" alt="Photo d'identité">
					{% else %}
						<img src="{{ absolute_url(asset('uploads/photos/default.png')) }}" alt="Photo d'identité par défaut">
					{% endif %}
				</p>
				<br/>
				<p>Nom Complet:
					<strong>{{ portefeuille.fullName }}</strong>
				</p>
				<p>Email:
					<strong>{{ portefeuille.email }}</strong>
				</p>
				<p>Numéro Téléphone:
					<strong>{{ portefeuille.phoneNumber }}</strong>
				</p>
			</div>
		</div>
	</body>
</html>
